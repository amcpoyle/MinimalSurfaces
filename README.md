# Minimal Surfaces
minimizer_numerical.py takes a surface element $f: U \rightarrow \mathbb{R}^{3}$ and optimizes it to a minimal surface ($H = 0$ everywhere in $U$) using CasADI.

minimizer.py was an attempt to perform surface minimization symbolically, but it was too slow so I switched to numerical methods.
